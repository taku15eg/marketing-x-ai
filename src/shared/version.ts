/**
 * Publish Gate — Schema Version
 *
 * Increment MINOR for additive changes (new optional fields).
 * Increment MAJOR when breaking changes require migration.
 */
export const SCHEMA_VERSION = '1.0.0' as const;
